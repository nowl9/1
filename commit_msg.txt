test(noarb): Phase 3 verification -- byte-identical replay, 19 tests, 151-record cross-validation

Phase 3 of the no-arb shadow goal: prove the shadow layer changes
nothing and pins its contract with tests.

Determinism regression (the goal's core promise): the banked
2026-06-01 window (hours 18-20, min_edge=0.03) was replayed once on
the pre-change tree (master @ 0a68e71) and once with the shadow layer
(7e2b894), projecting each ledger through the established semantic
projection (volatile stamp/identity columns dropped, floats rounded to
6 dp -- the same projection tests/test_feeds/test_replay.py uses).
The two projection files are BYTE-IDENTICAL (sha256
b971a49131a672cf...bdad4ef on both): the emitted signal set (1 order,
side=no, fill_adjusted_edge -0.149572), all 2345 rejections with their
fill-adjusted and chase-adjusted edges, 1 intent, 1 fill, 1 settlement
-- all exactly equal.  SHADOW MODE CHANGES NOTHING.

Cross-validation: the after-replay wrote exactly 151 noarb_shadow
records -- the precise count the Phase 1 probe measured for edges
computed on violating fits -- all calendar_vs_prev, all with a
positive would-be edge (max +0.961 on the deep-ITM 63000 digital), and
all signal_emitted=false: on this window the existing gates caught
every artifact edge anyway, and the one signal that DID emit rode a
clean fit.  That separation ("artifact edge that fired" vs "artifact
edge the gates caught") is exactly what the quiet-vs-CPI comparison
needs.

New tests (19; suite 822 -> 841 passed, 3m46s):
- tests/test_pricing/test_noarb.py (14): pure-function matrix.  Clean
  surface ok on both slices; Axel Vogt's butterfly-arbitrageable SVI
  params flag butterfly:g<0 at k=0.9 while checking CLEAN at ATM
  (strike-local, as it must be); a crossed calendar pair flags
  calendar_vs_prev on the longer slice and calendar_vs_next on the
  shorter (same crossing, both views); reason strings carry both w
  values for the later suppression goal's thresholding; sub-CAL_TOL
  jitter is NOT flagged; unfitted slice / missing expiry / bad forward
  / empty strikes / degenerate neighbour all return ok without
  crashing; the check mutates nothing.
- tests/test_main/test_noarb_shadow.py (5): pipeline contract through
  the REAL wiring (update_cache_from_surface -> run_scan_pipeline).  A
  clean surface logs NO record and the signal emits; a
  butterfly-violating smile and a calendar-violating pair each log
  EXACTLY ONE record with the right reason, strike, expiry, side, and
  positive would-be edge -- and the signal STILL emits
  (signal_emitted=True, signals_passed_filter=1); a clean refit clears
  the flag so later scans log nothing new (latest-fit semantics); the
  record round-trips JSONL with the run_id/mode stamp.

Scope proof: this commit is two new test files only (numstat
additive); the implementation commit (7e2b894) was +293/-0 across
pricing/noarb.py (new), paper_ledger.py, main.py.  No floor, gate,
fill model, DigitalPricer, or SVI-fit change anywhere in the goal.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
