test(recorder): C6 promote 2026-06-10 fault-injection repro into the suite

Promotes the Phase-1 diagnostic harness (outputs/diag_repro_disable.py)
into tests/test_feeds/test_recorder_fault_injection.py, pinning the
full incident signature against the NEW graceful-stop-loud behavior:

* six concurrent async streams; one injected ENOSPC (OSError errno 28,
  write AND close raising) on the polymarket handle -- the exact field
  trigger at 2026-06-10T08:01:25Z;
* all six streams stop together; the five healthy streams bank their
  buffered frames with VALID gzip trailers;
* the fatal alarm fires: on_fatal exactly once, RECORDER_FAILED
  sentinel names reason=io_error AND close_failures=polymarket (the
  swallowed-close defect stays dead);
* the RecorderWatchdog echoes exactly one kind=recorder_fatal alarm;
* subsequent record() calls no-op without raising, files stay frozen,
  the event loop stays alive, and a late close() (the 19:21 Ctrl-C
  analogue) is a safe no-op that touches no file.

Full suite: 874 passed, 0 failed.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
