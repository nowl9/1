docs(diag): PM history census + settlement calibration (3yr Polymarket BTC)

Adds docs/diag_pm_history_2026-06-13.md: sandboxed desk study over
120,526 closed Bitcoin-tagged Polymarket markets (2023-06..2026-06-12),
8,095 threshold binaries pulled at 5m via pmxt (8,666 calls, 0 errors;
discovery degraded to Gamma REST -- pmxt closed-search self-DoSes its
sidecar throttler).

P1 census: organic high-vol windows dwarf scheduled-macro ones ~30:1 at
DTE>=7 (mean 274 organic vs 9 scheduled 30m buckets/month at 2.5c/15m
over the last 12 full months); at DTE>=1 the tape saturates (75-93% of
clock). Recorder harvest is capacity-bound, not event-bound. 1m
refinement: 116/120 windows confirm; the 1.5c row is a lower bound.

P2 calibration (pre-registered): MARKET-WRONG at the mid level with a
DTE qualifier -- priced 0.60-0.80 settles YES 2.3-3.1pp below price
pooled (CI excludes price), -4.8pp at DTE 4-14, -10.6pp at DTE 15-42,
calibrated (+2.4pp) at DTE 1-3. Matches the bias-footprint +4.5pp YES
premium; decays into expiry. Placeholder-candle forensics (0.5/0.495
flat seed runs) documented; the verdict zone is unaffected by the filter.

Also corrects the 06-11 probe's phantom 2026-05-06 FOMC date (actual:
2026-04-29) and logs the new pmxt defect + candle chunk caps.

Sandboxed: no src changes; data/recordings untouched; artifacts
gitignored under outputs/pm_history/. Suite: 880 passed.
