docs(lowvol): C2 maker-side feasibility diagnostic on the banked quiet windows

docs/diag_lowvol_maker_2026-06-11.md -- P2-P5 read-only characterization
via a component harness (replay-mirroring decode + VolSurface/DigitalPricer/
ProbabilityCache/ContractMatcher; no Agent, no ReplayReader, no paper_ledger
writes; data/recordings opened read-only). Windows 0530/0601/0609 + 0611
partials; frame counts cross-validate the overnight sweep exactly.

Findings: quiet books are tight-but-thin at the touch (median 0.6-1c spread,
hundreds USD-proxy at touch, 5-20x within 1c -- the maker-side complement of
the measured 16-18pp taker haircut); Kalshi formed ZERO matched terminal
contracts in any window (maker fair-anchoring is Polymarket-only; feeds the
terminal-Kalshi open question); fair-anchored quotes at fair+/-1..3c are
ONE-SIDED (fair at/below best bid on ~65% of boundaries; 0 bid-side crosses
in 18,904 pairs) and RARELY interact (upper bound 0.35-0.75 crosses/ct-hr,
26/32 events from 0611); adverse-selection proxy shows zero pickoffs with
markout ~= offset (n=32 events -- consistent with, not a bound on, low
adverse selection). Epistemic header verbatim: no queue position, no fee
model, no inventory dynamics; upper-bound feasibility evidence, not P&L;
a maker build decision still requires the Stage 4 branch.

All headline numbers independently recomputed (second implementation) and
reproduced exactly, incl. one raw-frame end-to-end spot check. Docs only;
zero src changes; probe scripts + datasets under gitignored outputs/ and
analysis_out/.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
